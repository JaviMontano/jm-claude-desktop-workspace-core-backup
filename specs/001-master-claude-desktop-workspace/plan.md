# Plan

1. Extract reusable runtime from the source workspace into the core.
2. Isolate MetodologIA assets as an optional domain pack.
3. Seed desktop, Claude, Codex, capability, and account templates.
4. Add scripts for workspace bootstrap, shared sync, capability checks, and Antigravity export.
5. Bootstrap the local workspace instance from the core and snapshot live local configuration.
6. Run contract tests and capability smoke checks.

