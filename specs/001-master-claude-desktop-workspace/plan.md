# Plan

1. Extract reusable runtime from the source workspace into the core.
2. Isolate MetodologIA assets as an optional domain pack.
3. Seed desktop, Claude, Codex, capability, and account templates.
4. Add scripts for bootstrap, refresh, profile capture, doctor checks, shared sync, and Antigravity export.
5. Add compact assets plus repo-local skills and agents for maintenance and review.
6. Bootstrap the local workspace instance from the core and snapshot live local configuration.
7. Re-run verification from both the core and workspace perspectives.

## Acceptance Path

- A clean bootstrap creates a usable workspace without manual file surgery.
- A refresh preserves workspace-owned docs and rejects unsafe overwrite scenarios.
- A doctor run proves readiness without relying on unstated manual steps.
- Portable export and shared sync expose only the intended portable surface.
