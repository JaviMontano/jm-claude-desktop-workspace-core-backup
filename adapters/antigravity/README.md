# Antigravity Adapter

This directory is reserved for derived Antigravity exports.

Use `scripts/export-antigravity.sh` to generate the portable view. The generated output is intentionally excluded from git by default.

